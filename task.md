# 📋 异常放量策略回测与策略研究执行计划

- [x] **第 1 步：数据与环境验证**
  - [x] 编写并运行轻量级探测脚本，验证大盘指数（如中证1000 `sh000852`）与个股数据的日期对齐与字段名。
  - [x] 检验 DuckDB 读取 `data/*.parquet` 的速度与内存开销。

- [x] **第 2 步：编写核心回测计算引擎 (`backtester.py`)**
  - [x] 编写基于 DuckDB SQL 的月度异常放量识别因子（过去 12 个完整月，环比放大 $\ge 3$ 倍次数 $\ge 2$ 次）。
  - [x] 精确计算全市场股票的首次及第二次涨停日期（T 与 T_2）。
  - [x] 编写 Python/Pandas 级别的次日低吸成交匹配逻辑（第一次挂单与第二次挂单尝试，包含价格与时间限制）。
  - [x] 实现不同持有周期（20/40/60/120日）下的交易样本统计（收益率、胜率、区间最大回撤、以中证1000为基准 of Alpha 收益）。
  - [x] 编写组合模拟器（资金池 1000万，最大持仓 10 只，等权 10%，板块限制最多 2 只，中证1000 MA250 大势过滤，扣除双边 0.40% 佣金与滑点）。

- [x] **第 3 步：编写可视化报告生成器 (`run_backtest.py`)**
  - [x] 创建回测入口脚本，并发调用回测引擎。
  - [x] 自动生成流通市值分层矩阵统计、牛熊市场状态分层统计、板块收益统计等明细数据。
  - [x] 自动渲染并保存高颜值的 Markdown 回测战报报告 `月级异常放量策略回测报告.md`。
  - [x] 自动渲染并保存赛博朋克深色玻璃态 ECharts 仪表盘 `backtest_dashboard.html`，实现可视化曲线离线双击直开。

- [x] **第 4 步：运行并校验系统表现**
  - [x] 运行 `python run_backtest.py` 执行全市场历史回测。
  - [x] 检查并排除未来函数，校验成交率指标和收益率口径是否 100% 正确。
  - [x] 验证输出的 Markdown 报告与 HTML 交互式大屏展示。

- [x] **第 5 步：编写回测交付与总结文档 (`walkthrough.md`)**
  - [x] 记录回测核心数据（年化收益、胜率、最大回撤、最优持股周期、最优市值区间、风控优化效果等）。
  - [x] 绘制关键架构与成果关系图，完美结束回测工作。

- [x] **第 14 步：大盘均线大势过滤锁 (Market Regime Index MA Filter) 整合**
  - [x] 后端开发 (`server.py`)：实现动态均线滚动对账与成交率拦截。
  - [x] 前端开发 (`web/index.html`)：渲染大势过滤 Switch 开关卡片与防御绩效仪表盘。

- [x] **第 15 步：表头极致美化与个股止损风控退出策略（可选）集成**
  - [x] **后端开发 (`server.py`)**
    - [x] 在 `MonthlyAbnormalVolumeRequest` 中新增 `enable_stop_loss` 与 `stop_loss_pct` 两个风控因子。
    - [x] 在交易撮合循环的 1m~6m 持仓期内，增加每日 `low_adj` 止损价格对账。
    - [x] 完美实现止损触发时的早期终止出局逻辑，结算亏损比例与 stopped-out 平仓盈亏。
  - [x] **前端开发 (`web/index.html`)**
    - [x] 升级 `<thead>` 表头排版，将 `1m` 到 `6m` 均更换为极其精美的 Cyber Badge 卡片。
    - [x] 强化 `3m` 核心对比周期列，加装发光青色立面边框（border-x）与专属高亮。
    - [x] 在侧边栏新增赛博粉红风格玻璃态“🛡️ 退出策略风控锁 (可选)”控制面板。
    - [x] 提供止损 Switch 激活开关、亏损比例高精度滑块（支持 3%~25% 手动调节）及文字原理解说。
    - [x] 在 `runAbnormalVolumeBacktest` 中序列化止损相关参数并传给后端。
  - [x] **自研与系统集成编译 (`analyzer.py`)**
    - [x] 跑通 API 与编译器的集成调试，成功输出最新 `market_dashboard.html` standalone 文件。
